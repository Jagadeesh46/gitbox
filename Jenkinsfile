node {
   def mvnHome
   stage('Preparation') { // for display purposes
     
      echo "balaji 1"
     
   }
   stage('Build') {
      // Run the maven build
      echo "Building......"
     echo "vijay kumar"
   }
   stage('Results') {
       echo "balaji 3"
      build job:'gitpull'
      
   }
}
